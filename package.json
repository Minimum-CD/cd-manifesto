{
  "name": "minimumcd",
  "version": "1.0.0",
  "description": "Minimum Viable CD",
  "keywords": [
    "ci",
    "cd",
    "ci/cd",
    "mvp",
    "agile",
    "continuous",
    "integration",
    "delivery",
    "trunk-based",
    "trunk"
  ],
  "homepage": "https://minimumcd.org/minimumcd/",
  "bugs": {
    "url": "https://github.com/Minimum-CD/cd-manifesto/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Minimum-CD/cd-manifesto.git"
  },
  "license": "CC-BY-4.0",
  "author": "MinimumCD.org",
  "main": "hugo serve",
  "scripts": {
    "build": "hugo --minify",
    "build:clean": "rm -rf public && npm run build",
    "format:fix": "prettier --write '**/*.{js,yml,yaml,json,toml,md}'",
    "link-check": "npm run build:clean && linkinator --recurse public/ --config ./linkinator.config.json",
    "test:links": "linkinator ./public --config ./linkinator.config.json",
    "lint": "markdownlint ./content/**/*.md",
    "lint:fix": "markdownlint -f ./content/**/*.md",
    "postinstall": "node scripts/link-postcss-bin.mjs",
    "prepare": "husky",
    "start": "rm -rf public && hugo serve",
    "test": "npm run lint",
    "update": "npx npm-check-updates -u && npm install && hugo mod get -u ./... && hugo mod tidy"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.4",
    "husky": "^9.1.7",
    "linkinator": "^7.6.1",
    "markdownlint-cli": "^0.49.1",
    "postcss": "^8.5.20",
    "postcss-cli": "^11.0.1",
    "prettier": "^3.9.5",
    "prettier-plugin-go-template": "^0.0.15",
    "prettier-plugin-toml": "^2.0.6"
  }
}
